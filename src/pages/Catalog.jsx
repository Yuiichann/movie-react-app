import React from 'react';
import PropTypes from 'prop-types';

Catalog.propTypes = {
    
};

function Catalog(props) {
    return (
        <div>
            Category
        </div>
    );
}

export default Catalog;
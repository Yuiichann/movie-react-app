import React from 'react';
import PropTypes from 'prop-types';
import Button from '../components/Button/Button';

Home.propTypes = {
    
};

function Home(props) {
    return (
        <div>
            Home
        </div>
    );
}

export default Home;
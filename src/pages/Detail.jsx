import React from 'react';
import PropTypes from 'prop-types';

Detail.propTypes = {
    
};

function Detail(props) {
    return (
        <div>
            Detail
        </div>
    );
}

export default Detail;
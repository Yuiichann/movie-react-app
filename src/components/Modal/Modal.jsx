import React from 'react';
import PropTypes from 'prop-types';
import './Modal.scss';

Modal.propTypes = {
    
};

function Modal(props) {
    return (
        <div>
            
        </div>
    );
}

export default Modal;